name: Nuxt 3 tests

on:
  push:
    branches:
      - main
      - feat/*
      - fix/*
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  install:
    runs-on: ubuntu-latest
    name: Install Dependencies
    steps:
      - uses: actions/checkout@master
      - name: Install node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install dependencies
        run: npm install
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            .nuxt
          key: ${{ runner.os }}-node-nuxt-${{ hashFiles('**/package-lock.json') }}

  playwright:
    needs: install
    runs-on: ubuntu-latest
    name: Setup Playwright
    steps:
      - uses: actions/checkout@master
      - name: Restore cache
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            .nuxt
          key: ${{ runner.os }}-node-nuxt-${{ hashFiles('**/package-lock.json') }}
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps chromium

  test:
    needs: playwright
    runs-on: ubuntu-latest
    name: Run Tests
    steps:
      - uses: actions/checkout@master
      - name: Restore cache
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            .nuxt
          key: ${{ runner.os }}-node-nuxt-${{ hashFiles('**/package-lock.json') }}
      - name: Run tests
        run: npm run test

  build:
    needs: test
    runs-on: ubuntu-latest
    name: Build
    steps:
      - uses: actions/checkout@master
      - name: Restore cache
        uses: actions/cache@v4
        with:
          path: |
            node_modules
            .nuxt
          key: ${{ runner.os }}-node-nuxt-${{ hashFiles('**/package-lock.json') }}
      - name: Build
        run: npm run build
